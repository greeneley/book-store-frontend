module.exports = {
    content: ['./src/**/*.html', './src/**/*.{js,jsx,ts,tsx}'],
    darkMode: 'media', // or 'media' or 'class'
    theme: {
        extend: {
            colors: {
                'primary-color': '#FFCE1A'
            }
        }
    },
    variants: {
        extend: {}
    },
    plugins: []
};
